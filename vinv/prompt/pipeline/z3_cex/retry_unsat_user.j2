Previous attempt failed with error:
{{ last_error_msg }}

Please adjust the constraints to make the failing condition satisfiable (prefer minimal constraints). Ensure type ranges and vector modeling are respected, then resend ONLY the corrected Python code.
