You are an expert in Rust/Verus and the Python Z3 API. Given an assume/assert-converted program, emit a Python script that finds a model where all assumes hold and at least one assert fails, then set required globals.
